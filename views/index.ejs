<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Greatest Songs</title>
  <link rel="shortcut icon" href="favicon.ico">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css" />
  <link rel="stylesheet" href="font-awesome.min.css">
  <link rel="stylesheet" href="styles.css">
</head>

<body>
  <h1>Greatest Songs Of All Time</h1>
  <h2>Enter A Song And Whichever One Has The Most Stars Is The Greatest</h2>


  <h3>Songs</h3>
  <ul class="messages">
    <% for(var i=0; i<messages.length; i++) {%>
      <li class="message">
        <span>
          <%= messages[i].name %>
        </span>
        <span>
          <%= messages[i].msg %>
        </span>
          <div class="stars">
            <i class="fa-solid fa-star <%= messages[i].star >= 0 ? 'active' : '' %>" data-id="<%= messages[i]._id %>"></i>
            <i class="fa-solid fa-star <%= messages[i].star >= 1 ? 'active' : '' %>" data-id="<%= messages[i]._id %>"></i>
            <i class="fa-solid fa-star <%= messages[i].star >= 2 ? 'active' : '' %>" data-id="<%= messages[i]._id %>"></i>
            <i class="fa-solid fa-star <%= messages[i].star >= 3 ? 'active' : '' %>" data-id="<%= messages[i]._id %>"></i>
            <i class="fa-solid fa-star <%= messages[i].star >= 4 ? 'active' : '' %>" data-id="<%= messages[i]._id %>"></i>
            <!-- <%= messages[i].star %> -->
          </div>
          <span><i class="fa fa-trash" aria-hidden="true" data-id="<%= messages[i]._id %>"></i></span>
      </li>
      <% } %>
  </ul>

  <h2>Enter An Artist & Song!</h2>

  <form action="/messages" method="POST">
    <input type="text" placeholder="Artist" name="name">
    <input type="text" placeholder="Song" name="msg">
    <button type="submit">Submit</button>
  </form>

  <script src="main.js"></script>

</body>

</html>